$('#box').resizable({ maxHeight: 200, minHeight: 100, maxWidth: 200, minWidth: 100 }); 
// .resizable({ handles: 'all' }); // The letters represent the 4 cardinal points i.e, East, SouthEast, North etc
// .resizable({ grid: [50, 50] });
// .resizable({ animate: true, ghost: true, aspectRatio: true, autoHide: true });
// .resizable({ animate: true, animateDuration: 3000, animateEasing: 'linear', ghost: true });